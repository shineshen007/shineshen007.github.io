---
layput: post
title: Markdown基础知识
categories: [Writing]
tags: [idea]
---
***

## 缘由
经常写博客需要用到markdown，记录一些语法，方便查看。

## 标题
```
# 这是一级标题
## 这是二级标题
### 这是三级标题
#### 这是四级标题
以此类推,#符号后面要空一格
```
## 换行
在句尾空格  
两个字节，另起一行书写

## 字体
```
**这是加粗的文字**
*这是倾斜的文字*`
***这是斜体加粗的文字***
~~这是加删除线的文字~~
```

## 图片
```
![图片alt](图片地址（网页地址比较稳定） ''图片title'')

图片alt就是显示在图片下面的文字，相当于对图片内容的解释。
图片title是图片的标题，当鼠标移到图片上时显示的内容。title可加可不加
```
![概率](https://pic2.zhimg.com/v2-9f323435fe96ed054b4f34b6f85012ac_1200x500.jpg)

## 超链接
```
[超链接名](超链接地址 "超链接title")
title可加可不加
```
[转载自简书](https://www.jianshu.com/p/191d1e21f7ed)

## 表格

姓名|技能|排行
--|:--:|--:
刘备|哭|大哥
关羽|打|二哥
张飞|骂|三弟

# 代码
    `单行代码内容`
    
 ```
  代码块...
  代码块...
  代码块...
```
1. 标题

在行首使用#表示标题，标题级别由#的数量决定，最多支持六级标题。例如：
```
# 一级标题
## 二级标题
### 三级标题
```

2. 段落和换行

方法论：

段落由一个或多个连续的文本行组成。<font color=red>如果要换行，只需在行末加两个以上的空格，然后回车。</font>
<div style="background-color: #F0f8ff; padding: 10px">
     如果要书写新的段落，只需要在段落最后一行的行末，点击两次回车键。
</div>
3. 文字格式

在书写文字的时候，我们需要对文字的格式进行一些修改，或者需要对重点内容进行标注以表示强调。对文字格式的变换包括：加粗、斜体、更改颜色、更改字体、高亮、上下标、增加下划线等等。

上述的功能在Markdown语法中都是支持的。

3.1 加粗/斜体

```
*斜体文字*
**粗体文字**

斜体和加粗可以同时使用
***加粗与斜体***
```

3.2 字体种类/颜色/大小

字体类型
参数face表示设置字体类型
示例：
```
<font face=楷体>楷体字</font>
<font face=黑体>黑体字</font>
<font face=STCAIYUN>华文云彩</font>
```

字体颜色
参数color表示设置字体颜色, 可以使用颜色英文名或者十六进制颜色值
示例：
```
<font color=blue>蓝色</font>
<font color=#008000>绿色</font>
<font color=red>红色</font>
```

字体大小
参数size表示设置字体大小, 取值范围是1-7, 数字越大字体越大
示例：
```
<font size=5>5号字</font>
<font size=8>8号字</font>
```

字体类型\颜色\大小可以同时设置

font指令中同时设置face、color和size参数
示例：
```
<font face=楷体 color=green size=4>楷体绿色4号字</font> 
```

3.3 背底色

背景颜色
示例：
```
<div style="background-color: #F0f8ff; padding: 10px">
     这是一个带背景颜色的文本
</div>
```
文字底色

示例：
```
<span style="background-color: plum;font-size:60%">这部分文字有紫色背景。</span>
<span>指令中使用的设置参数如下：

font-style：将样式应用于给定文本。文本应为正常、斜体、首字母等。
font-family： 用于将给定列表中的不同字体类型应用于特定文本。
font-size： 调整字体大小。
font-weight： 此属性用于设置粗体或粗字体。
text-transform： 它将使文本大写。
text-decoration： 此属性用于以文本修饰行、文本修饰颜色等形式修饰文本。
color： 用于为文本内容和文本修饰着色的 span 标记的此属性。
background-color： 这是设置元素的背景色的有用属性。
text-shadow：此属性允许用户向文本添加阴影。
text-align-last： 这将有助于对齐文本。
word-spacing： span 标记中的此属性用于管理单词之间的间距。
white-space：此属性帮助我们处理指定元素内的空格。
line-height： 它在 HTML 代码中提供行的高度。
word-break： 此属性有助于我们定义实际行应在何处断开。
text-overflow：这是 span 标记最有用的属性，它有助于我们识别未显示的溢出内容，这些内容应向用户发出信号。
```
此处引用CSDN文章：https://blog.csdn.net/allway2/article/details/127405553


3.4 下划线/删除线

下划线
示例：
```
<u>x下划线</u>
```
删除线
示例：
```
~~删除线~~
```
3.5 上下标

上标
示例：
```
H<sub>2</sub>O 
10<sup>3</sup>
```

4. 列表

Markdown支持有序列表和无序列表
4.1 无序列表

使用星号*、加号+或是减号-作为列表标记，这些标记后面要添加一个空格，然后再写内容。
例如：
```
* 第一项
* 第二项
* 第三项

(*, +, -显示的效果相同)
```
4.2 有序列表

有序列表使用数字加上. 再打一个空格即可。
例如：
```
1. 第一项
2. 第二项
3. 第三项
```

4.3 列表嵌套

嵌套列表只需要在子列表前面打上两个或四个空格即可（可使用Tab键）。
子列表若是有序列表，则会自动调整成其他字母表示顺序。
例如：
```
1. 第一项
    1. 第一项的第一个元素
    2. 第一项的第二个元素
```
子列表若是无序列表，则会自动调整成其他符号表示。
例如：
```
- 第二项
    - 第二项的第一个元素
    - 第二项的第二个元素
```
表格

基本语法
使用|分隔单元格，使用-创建表头分隔线。例如：

| 表头1 | 表头2 |
|-------|-------|
| 单元格1 | 单元格2 |
| 单元格3 | 单元格4 |


文字对齐方式
-:表示内容与标题右对齐
:-表示内容与标题左对齐
:-:表示内容与标题居中
例如：

| 表头1 | 表头2 |
|:-------|:-------|
| 单元格1 | 单元格2 |
| 单元格3 | 单元格4 |

